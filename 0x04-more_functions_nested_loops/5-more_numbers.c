#include "main.h"

void more_numbers(void)
{
	int i = 0;

	while (i <= 14)
	{
		int lastDigit = j % 10;

		it (j > 9)
		{
			int firstDigit = j/10;
			_putchar(firstDigit + '0');
		}
		_putchar(lastDigit + '0');
		j++;
	}
	_putchar('\n');
	i++;
}
}
